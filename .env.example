# Required
ANTHROPIC_API_KEY=your-anthropic-api-key

# Required for chat/retrieval
VOYAGE_API_KEY=your-voyage-api-key

# Optional: override SQLite path (default: /data/sessions.db inside container)
# DB_PATH=/data/sessions.db
